# Mesa IV Burgers — Frontend

Aplicação independente construída com React, TypeScript e Vite.

```bash
npm install
cp .env.example .env
npm run dev
```

O frontend deve possuir somente `VITE_API_URL`. Credenciais do MongoDB, JWT e Mercado Pago nunca devem ser colocadas aqui.
