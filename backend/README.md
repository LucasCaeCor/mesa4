# Mesa IV Burgers — Backend

API independente construída com Fastify, TypeScript, Prisma e MongoDB.

```bash
npm install
cp .env.example .env
npm run prisma:generate
npm run prisma:push
npm run seed
npm run dev
```

Nunca envie o arquivo `.env` ao GitHub.
